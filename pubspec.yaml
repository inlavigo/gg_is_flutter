name: gg_is_flutter
description: Provides a function that detects if a directory contains a flutter project.
version: 1.0.7
repository: https://github.com/inlavigo/gg_is_flutter.git

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  gg_project_root: ^1.0.3
  path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  pana: ^0.22.2
  test: ^1.24.0